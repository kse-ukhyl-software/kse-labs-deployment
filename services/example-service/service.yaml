apiVersion: v1
kind: Service
metadata:
  name: example-service
  namespace: example-service
  labels:
    app.kubernetes.io/name: example-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: example-service
